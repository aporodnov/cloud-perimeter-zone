name: 1. Deploy-Peering-PerimeterLandingZone

on:
    workflow_dispatch:

env:
    LOCATION: "canadacentral"
    SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

permissions:
  id-token: write
  contents: read

jobs:
    Deploy_Peering_for_PerimeterLandingZone:
        runs-on: ubuntu-latest
        environment: ${{ github.ref == 'refs/heads/tst' && 'tst' || github.ref == 'refs/heads/prd' && 'prd' || github.ref == 'refs/heads/main' && 'prd' || 'dev' }}
        steps:
            - name: Checkout Repository
              uses: actions/checkout@v4
              with:
                fetch-depth: 0

            - name: Azure Login
              uses: azure/login@v2
              with:
                client-id: ${{ secrets.AZURE_CLIENT_ID }}
                tenant-id: ${{ secrets.AZURE_TENANT_ID }}
                subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

            - name: Set Az Context
              uses: azure/cli@v2
              with:
                azcliversion: latest
                inlineScript: |
                    az account set --subscription ${{ secrets.AZURE_SUBSCRIPTION_ID }}
                    az account show

            - name: Deploy Peering for Perimeter Landing Zone
              uses: azure/cli@v2
              with:
                azcliversion: latest
                inlineScript: |
                    az deployment sub create \
                        --name "Deploy_Peering_For_PLZ" \
                        --location $LOCATION \
                        --template-file modules/perimeterZone.bicep \
                        --parameters config/perimeterZone/perimeterZone.bicepparam