{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "scope": {
      "type": "string",
      "title": "Deployment Scope",
      "description": "Defines the scope of the deployment, either at the subscription or resource group level.",
      "default": "subscription",
      "enum": [
        "subscription",
        "resourceGroup"
      ]
    },
    "subscriptionId": {
      "type": "string",
      "title": "Subscription ID",
      "description": "The ID of the Azure subscription where the resources will be deployed."
    },
    "resourceGroupName": {
      "type": "string",
      "title": "Resource Group Name",
      "description": "The name of the resource group where the resources will be deployed."
    },
    "location": {
      "type": "string",
      "title": "Location",
      "description": "The Azure region where the resources will be deployed."
    },
    "tags": {
      "type": "object",
      "title": "Tags",
      "description": "Tags to apply to the resources for categorization and management.",
      "additionalProperties": true
    }
  },
  "required": [
    "scope",
    "subscriptionId",
    "location"
  ],
  "allOf": [
    {
      "if": {
        "properties": {
          "scope": {
            "const": "resourceGroup"
          }
        }
      },
      "then": {
        "required": [
          "resourceGroupName",
          "tags"
        ]
      }
    }
  ],
  "additionalProperties": false
}